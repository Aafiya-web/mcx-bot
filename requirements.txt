# Core (paper mode runs with just these):
requests>=2.31
python-dotenv>=1.0
pytest>=8.0
pandas>=2.2
numpy>=1.26
Flask>=3.0

# Broker connectivity (lazy-imported; unused in mock mode but must be
# installed — run_bot switches to LiveFeed the moment .env has creds):
smartapi-python>=1.4
pyotp>=2.9
